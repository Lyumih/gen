namespace $ {
	export class $gen_engine_item_buff extends $gen_engine_item {

		override type() {
			return 'buff'
		}

		/** Метод для использования данного умения */
		use( source: $gen_engine_item_unit, targets: $gen_engine_item_unit[] ) {
		}
	}
}
